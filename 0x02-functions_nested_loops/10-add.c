#include "./main.h"

/**
 * add - add two integers
 * Return: result of addition
 * @a: a is one the integers to add (parameter)
 * @b: b is the other int to add
 */
int add(int a, int b)
{
	return (a + b);
}
