#include <stdio>

/**
 * main - entry point
 * print all single digit number ofs base 10 from 0
 * using putchar
 * Return: Always 0 (success)
 */
int main(void)
{
int n;
	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
